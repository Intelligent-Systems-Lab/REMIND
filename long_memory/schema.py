# Memory schema in the Weaviate
CLASS_SCHEMA = {
    "class": "base",
    "properties": [
        {
            "name": "time",
            "dataType": ["date"]
        },
        {
            "name": "text",
            "dataType": ["text"]
        }
    ]
}